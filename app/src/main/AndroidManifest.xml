<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.unc.tomas.assignment3">

    <uses-feature android:name="android.hardware.sensor.accelerometer"
        android:required="true" />
    <uses-feature android:name="android.hardware.sensor.light"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".SensorChooserActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AccelerometerDataActivity"
            android:label="@string/accelerometer_title"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".LightSensorDataActivity"
            android:label="@string/light_sensor_title"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".AccelerometerAnimationActivity"
            android:label="@string/accelerometer_title"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".LightSensorAnimationActivity"
            android:label="@string/light_sensor_title"
            android:screenOrientation="portrait"/>

    </application>

</manifest>